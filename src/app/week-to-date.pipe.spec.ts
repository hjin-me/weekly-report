import { WeekToDatePipe } from './week-to-date.pipe';

describe('WeekToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
